# Reload the shell
reload() {

    if [[ -f ~/.bash_profile ]] ; then
        source ~/.bash_profile > /dev/null 2>&1
    fi

    if [[ -f ~/.tmux.conf ]] ; then
        tmux source-file ~/.tmux.conf > /dev/null 2>&1
    fi
}