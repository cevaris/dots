#!/usr/bin/env bash
set -e

pad='.........................'

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

for f in $(ls -a $DIR | grep '^\.[a-zA-Z0-9]\{2,\}')
do
  printf "Linking %s %s" $f "${pad:${#f}}"
  ln -fs "$DIR/$f" ~/$f
  printf "done.\n"
done
