#!/usr/bin/env bash
line='.........................'
DONE='done'

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

for f in $(ls -a $DIR | grep '^\.[a-zA-Z0-9]\{2,\}')
do
  printf "Linking %s %s done.\n" $f "${line:${#f}}" 
done
